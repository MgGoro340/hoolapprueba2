stages:
    - deploy
build:
    stage: deploy
    cache:
      paths:
        - node_modules/
    script:
      - 'echo "hello, llamando affff docker!"'
      - 'echo "Start building vvApp"'
      - npm install
      - npm build
      - docker build . -t gitlabbadquest:latest
      - echo "Build successfully!"
    image: node
    services:
      - docker:dind  